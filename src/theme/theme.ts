import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  styles: {
    grobal: {
      body: {
        backgroundColor: "gray.１00",
        color: "gray.800"
      }
    }
  }
});

export default theme;
